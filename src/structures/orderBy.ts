/**
 * Enumeration for the types of ordering.
 * The key is the value attribute in the <option> element, and the value is the text content.
 */
export enum OrderBy {
	name = "Name",
	mobileNumber = "Mobile Number",
}
